# mobile examples

这里放 `jue` 面向移动端场景的独立示例。

当前包含：

- `jue-mobile-showcase`
  - 同一份 TSX authoring source
  - 一个浏览器可跑的 web mount
  - 一个微信小程序用的 skyline page scaffold

这些示例故意不接入 `examples/web-playground`。
它们更接近“单独拿 jue 做一个独立项目”的目录形态。
