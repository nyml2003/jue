{
  "name": "@jue/web-playground",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@jue/shared": "workspace:*",
    "@jue/web": "workspace:*"
  },
  "scripts": {
    "build": "pnpm exec tsx ../../../scripts/compile-example-components.ts && pnpm exec tsx ./scripts/build-example-apps.ts",
    "preview": "pnpm run build && pnpm exec tsx ./scripts/serve-built-example-apps.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:e2e": "pnpm run build && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "terser": "^5.46.1"
  }
}
