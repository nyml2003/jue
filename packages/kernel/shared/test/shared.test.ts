import { describe, expect, it } from "vitest";  import { BindingOpcode, err, HOST_EVENT_KEYS, HOST_PRIMITIVES, Lane, ok, RegionLifecycle, RegionType } from "../src/index";  describe("@jue/shared", () => {   it("exposes stable enum values for the initial scaffold", () => {     expect(BindingOpcode.TEXT).toBe(0);     expect(RegionType.CONDITIONAL).toBe(0);     expect(Lane.VISIBLE_UPDATE).toBe(1);     expect(RegionLifecycle.ACTIVE).toBe(2);   });    it("exposes host primitive metadata", () => {     expect(HOST_PRIMITIVES).toContain("View");     expect(HOST_EVENT_KEYS).toContain("onPress");   });    it("creates Result helpers", () => {     const okResult = ok(42);     const errResult = err({ code: "FAIL" });      expect(okResult).toEqual({       ok: true,       value: 42,       error: null     });     expect(errResult).toEqual({       ok: false,       value: null,       error: { code: "FAIL" }     });   }); });